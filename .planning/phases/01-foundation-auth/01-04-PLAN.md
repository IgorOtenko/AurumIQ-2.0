---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - .gitignore
  - .pre-commit-config.yaml
  - README.md
  - docs/architecture.md
  - tests/unit/auth.test.ts
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "git-secrets pre-commit hook blocks commits containing AWS key patterns or generic secret patterns"
    - "README.md documents project setup, local dev, and architecture overview"
    - "Unit tests verify signup validation, password hashing, and reset token logic"
    - ".gitignore includes .env.local, node_modules, .next, and other sensitive/generated paths"
  artifacts:
    - path: "README.md"
      provides: "Project documentation for interviews"
      contains: "AurumIQ"
      min_lines: 50
    - path: "docs/architecture.md"
      provides: "Architecture documentation"
      contains: "Auth"
    - path: "tests/unit/auth.test.ts"
      provides: "Auth unit tests"
      contains: "describe"
    - path: ".gitignore"
      provides: "Git ignore rules"
      contains: ".env.local"
  key_links:
    - from: "tests/unit/auth.test.ts"
      to: "src/app/api/auth/signup/route.ts"
      via: "Tests signup validation logic"
      pattern: "signup"
---

<objective>
Harden security with git-secrets hook, add unit tests for auth logic, and create interview-quality documentation.

Purpose: Closes INFRA-03 (documentation) and adds security guardrails (Pitfall 5 prevention). Unit tests verify the auth logic built in Plans 02 and 03.
Output: git-secrets configured, README + architecture docs, auth unit tests passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/SKELETON.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git-secrets hook, .gitignore hardening, and auth unit tests</name>
  <files>.gitignore, tests/unit/auth.test.ts, vitest.config.ts</files>
  <read_first>
    - .gitignore (current state -- must preserve existing entries and add security patterns)
    - vitest.config.ts (current config -- may need path aliases for test imports)
    - src/app/api/auth/signup/route.ts (understand Zod validation and bcrypt hashing to test)
    - src/app/api/auth/reset-password/route.ts (understand reset token generation to test)
    - src/app/api/auth/reset-password/confirm/route.ts (understand token validation to test)
    - src/lib/env.ts (understand env validation pattern)
  </read_first>
  <action>
    Update .gitignore to ensure these patterns are present (add if missing):
    - .env, .env.local, .env.*.local (all env files with secrets)
    - node_modules/, .next/, out/
    - *.pem, *.key (certificate files)
    - .vercel, .turbo
    - coverage/

    Attempt to install git-secrets: `git secrets --install` and add AWS patterns via `git secrets --register-aws`. If git-secrets is not available on the system (Windows), create a note in README.md documenting the recommended setup. Add custom patterns for generic secrets: `git secrets --add 'NEXTAUTH_SECRET=(?!your-secret-here|placeholder).'` to catch hardcoded secrets. If git-secrets CLI is not available, skip the hook installation gracefully and document it as a recommended setup step.

    Create tests/unit/auth.test.ts with Vitest:
    - Import bcryptjs (hash, compare)
    - Import zod schemas if they are extracted, or recreate the validation inline
    - Test group "Signup Validation":
      - Test: valid email and password (8+ chars) passes validation
      - Test: invalid email format fails validation
      - Test: password shorter than 8 chars fails validation
    - Test group "Password Hashing":
      - Test: bcryptjs.hash produces a hash starting with "$2a$" or "$2b$"
      - Test: bcryptjs.compare returns true for correct password
      - Test: bcryptjs.compare returns false for incorrect password
    - Test group "Reset Token":
      - Test: crypto.randomUUID() produces a valid UUID format
      - Test: token expiry check logic (date comparison: expiresAt < now means expired)

    These are pure unit tests -- no DB calls, no HTTP. They validate the core auth logic in isolation. Prisma mocking is not needed because we test the validation and hashing functions directly, not the route handlers.

    Ensure vitest.config.ts has the correct setup for running these tests (path aliases, test include pattern).
  </action>
  <verify>
    <automated>cd "C:/Users/97254/AurumIQ 2.0" && npx vitest run tests/unit/auth.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains ".env.local" and "*.pem" and "node_modules"
    - tests/unit/auth.test.ts contains at least 6 test cases across 3 describe blocks
    - All tests pass with `npx vitest run tests/unit/auth.test.ts`
    - Tests cover: email validation, password length validation, bcrypt hash format, bcrypt compare true/false, UUID format, expiry date comparison
  </acceptance_criteria>
  <done>Security gitignore hardened, git-secrets documented/installed, auth unit tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Interview-quality README and architecture documentation</name>
  <files>README.md, docs/architecture.md</files>
  <read_first>
    - package.json (exact dependency list and scripts for README)
    - prisma/schema.prisma (models for architecture doc)
    - src/lib/auth.ts (auth architecture for docs)
    - src/middleware.ts (route protection for docs)
    - SKELETON.md (architectural decisions to reference)
    - CLAUDE.md "Presentation-Grade Documentation" section
  </read_first>
  <action>
    Create README.md (interview-quality, 80+ lines):
    - Project title: AurumIQ
    - Tagline: "AI-Powered Financial Analysis Platform"
    - Brief description (2-3 sentences matching PROJECT.md core value)
    - Tech Stack section: table of key technologies with versions (Next.js 15, React 19, TypeScript, Tailwind v4, PostgreSQL, Prisma, Auth.js v5)
    - Getting Started section:
      - Prerequisites: Node.js 20+, pnpm, PostgreSQL
      - Clone, install, env setup (reference .env.example)
      - Database setup: npx prisma db push
      - Run: pnpm dev
    - Project Structure: key directories explained (src/app, src/lib, src/components, prisma, tests)
    - Authentication: brief description of auth flow (signup, login, JWT sessions, route protection)
    - Available Scripts: pnpm dev, pnpm build, pnpm test, npx prisma studio
    - Architecture section: link to docs/architecture.md
    - License placeholder
    - Do NOT include badges, emojis, or decorative elements -- clean professional style

    Create docs/architecture.md:
    - Title: "AurumIQ Architecture"
    - Overview: Next.js 15 full-stack monolith, App Router, API Route Handlers
    - Authentication Architecture:
      - Auth.js v5 with Credentials provider
      - JWT sessions in httpOnly cookies
      - Middleware-based route protection on /dashboard/*
      - Password hashing: bcryptjs with cost factor 12
      - Password reset: token-based, 1-hour expiry, single-use
    - Database:
      - PostgreSQL via Prisma ORM
      - Current models: User, PasswordResetToken
      - JSONB columns planned for AI analysis output (Phase 5)
    - Security:
      - Server-only environment validation (@t3-oss/env-nextjs)
      - No API keys in frontend bundle
      - git-secrets pre-commit hook (recommended)
      - bcrypt password storage
      - httpOnly JWT cookies
    - Directory Layout (from SKELETON.md)
    - Future Architecture (brief -- Skill/Plugin pattern for Phase 5, SSE for real-time updates)
    - Keep it factual, concise, well-structured. This is interview material.
  </action>
  <verify>
    <automated>cd "C:/Users/97254/AurumIQ 2.0" && wc -l README.md docs/architecture.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists with 80+ lines
    - README.md contains sections: Tech Stack, Getting Started, Project Structure, Authentication, Available Scripts
    - README.md references .env.example and "npx prisma db push" in setup instructions
    - docs/architecture.md exists with 60+ lines
    - docs/architecture.md contains sections on Authentication Architecture, Database, Security
    - docs/architecture.md mentions httpOnly cookies, bcryptjs, @t3-oss/env-nextjs
    - Neither file contains placeholder text like "TODO" or "coming soon" for Phase 1 features
  </acceptance_criteria>
  <done>Interview-quality README and architecture documentation created. INFRA-03 (Phase 1 scope) satisfied. Documentation accurately describes the auth system, tech stack, and security architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> git | Pre-commit hook validates no secrets in staged files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Information Disclosure | git commits | mitigate | git-secrets hook blocks AWS key patterns and custom NEXTAUTH_SECRET patterns; .gitignore excludes .env.local |
| T-01-13 | Information Disclosure | README.md | mitigate | README references .env.example (no secrets) not .env.local; setup instructions do not contain actual credentials |
| T-01-SC | Tampering | npm installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
1. All auth unit tests pass: `npx vitest run tests/unit/auth.test.ts`
2. README.md is professional and complete (no TODOs for Phase 1 content)
3. docs/architecture.md accurately describes current auth system
4. .gitignore blocks .env.local from being committed
5. `pnpm build` still passes after all changes
</verification>

<success_criteria>
- 6+ auth unit tests passing
- README with setup instructions a new developer can follow
- Architecture doc describing auth flow, security measures, and database schema
- .gitignore hardened against secret leaks
- git-secrets installed or documented as recommended setup
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md` when done
</output>
