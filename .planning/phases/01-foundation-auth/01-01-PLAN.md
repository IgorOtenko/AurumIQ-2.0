---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - .eslintrc.json
  - .prettierrc
  - .env.example
  - .env.local
  - src/lib/env.ts
  - src/lib/db.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - prisma/schema.prisma
  - vitest.config.ts
  - playwright.config.ts
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "pnpm dev starts the Next.js dev server without errors on http://localhost:3000"
    - "The root page renders with a dark background (not white)"
    - "Prisma schema contains a User model with id, email, hashedPassword, name, createdAt, updatedAt"
    - "npx prisma db push succeeds against a local PostgreSQL database"
    - "Environment validation fails at startup if DATABASE_URL is missing"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "Type-safe environment validation"
      contains: "createEnv"
    - path: "src/lib/db.ts"
      provides: "Prisma client singleton"
      exports: ["db"]
    - path: "prisma/schema.prisma"
      provides: "User model definition"
      contains: "model User"
    - path: "src/app/globals.css"
      provides: "Dark theme CSS tokens"
      contains: "dark"
    - path: "src/app/layout.tsx"
      provides: "Root layout with dark class"
      contains: "dark"
  key_links:
    - from: "src/lib/db.ts"
      to: "prisma/schema.prisma"
      via: "PrismaClient import"
      pattern: "PrismaClient"
    - from: "src/lib/env.ts"
      to: ".env.local"
      via: "@t3-oss/env-nextjs"
      pattern: "DATABASE_URL"
---

<objective>
Scaffold Next.js 15 project with Prisma User schema, dark theme tokens, and typed env validation -- the foundation every subsequent plan builds on.

Purpose: Establish the walking skeleton's base layer. After this plan, the project builds, runs, connects to PostgreSQL, and renders dark.
Output: A runnable Next.js 15 app with Prisma User model pushed to DB, dark theme CSS, and @t3-oss/env-nextjs validation.
</objective>

## Phase Goal

**As a** developer, **I want to** have a scaffolded full-stack project with auth infrastructure and dark theme, **so that** I can build all subsequent features on a secure, styled foundation.

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .eslintrc.json, .prettierrc, vitest.config.ts, playwright.config.ts</files>
  <read_first>
    - SKELETON.md for directory layout and dependency decisions
    - CLAUDE.md for the full technology stack table (exact versions)
  </read_first>
  <action>
    Run `pnpm create next-app@latest aurumiq --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in the repo root (or equivalent non-interactive flags). The project files should land directly in the repo root, not a nested `aurumiq/` subfolder -- if create-next-app creates a subfolder, move contents up.

    After scaffold, install Phase 1 dependencies in a single pnpm add command:
    - Production: `prisma @prisma/client bcryptjs @t3-oss/env-nextjs next-auth@beta zod`
    - Dev: `@types/bcryptjs vitest @vitejs/plugin-react playwright @playwright/test prettier prettier-plugin-tailwindcss`

    Note: `next-auth@beta` is the Auth.js v5 package for Next.js 15 App Router compatibility. Do NOT install `next-auth@4`.

    Configure vitest.config.ts with `@vitejs/plugin-react` plugin and path aliases matching tsconfig paths (`@/*` -> `./src/*`).

    Configure playwright.config.ts with baseURL `http://localhost:3000`, webServer command `pnpm dev`.

    Configure .prettierrc with `prettier-plugin-tailwindcss` plugin and consistent settings (semi: true, singleQuote: true, tabWidth: 2).

    Ensure tsconfig.json has `strict: true` in compilerOptions.

    In next.config.ts, set `serverExternalPackages: ['bcryptjs']` to avoid edge runtime issues.
  </action>
  <verify>
    <automated>cd "C:/Users/97254/AurumIQ 2.0" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next": "15" (major version), "prisma", "@prisma/client", "bcryptjs", "@t3-oss/env-nextjs", "next-auth", "zod"
    - tsconfig.json has "strict": true
    - vitest.config.ts exists with path alias "@/*"
    - playwright.config.ts exists with baseURL "http://localhost:3000"
    - `pnpm build` completes with exit code 0
  </acceptance_criteria>
  <done>Next.js 15 project scaffolded with all Phase 1 dependencies installed, TypeScript strict mode, Vitest and Playwright configured, Prettier with Tailwind plugin configured.</done>
</task>

<task type="auto">
  <name>Task 2: Prisma User schema, env validation, dark theme, and DB push</name>
  <files>prisma/schema.prisma, src/lib/env.ts, src/lib/db.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, .env.example, .env.local</files>
  <read_first>
    - SKELETON.md for User model fields, env var list, and directory layout
    - src/app/layout.tsx (created by scaffold -- must see current state before modifying)
    - src/app/globals.css (created by scaffold -- must see current Tailwind directives)
    - CLAUDE.md "Dark Theme Implementation" pattern reference
  </read_first>
  <action>
    Create prisma/schema.prisma with:
    - datasource db: provider "postgresql", url from env("DATABASE_URL")
    - generator client: provider "prisma-client-js"
    - model User: id (String, uuid, @id, @default(uuid())), email (String, @unique), hashedPassword (String), name (String, optional), createdAt (DateTime, @default(now())), updatedAt (DateTime, @updatedAt)
    - Add @@map("users") to User model for conventional table naming

    Create src/lib/env.ts using createEnv from @t3-oss/env-nextjs:
    - server: DATABASE_URL (z.string().url()), NEXTAUTH_SECRET (z.string().min(32)), NEXTAUTH_URL (z.string().url().optional())
    - client: {} (empty -- no client env vars in Phase 1)
    - runtimeEnv mapping each to process.env.*

    Create src/lib/db.ts: Prisma client singleton pattern using globalThis to prevent hot-reload connection exhaustion. Export as `db`. Import and validate env from @/lib/env to ensure DATABASE_URL is present.

    Create .env.example with all env var keys (no values) and comments explaining each.
    Create .env.local with DATABASE_URL pointing to local PostgreSQL: postgresql://postgres:postgres@localhost:5432/aurumiq, and NEXTAUTH_SECRET as a placeholder (instruct user to generate with `openssl rand -base64 32`), NEXTAUTH_URL=http://localhost:3000.

    Update src/app/globals.css: Keep Tailwind directives. Add CSS custom properties for the financial dark theme:
    - --background: 222.2 84% 4.9% (dark navy/slate from screenshots)
    - --foreground: 210 40% 98% (light text)
    - --card: 222.2 84% 6.9%
    - --positive: 142 76% 36% (green for gains)
    - --negative: 0 84% 60% (red for losses)
    - --accent: 189 94% 43% (cyan accent from screenshots)
    Use the shadcn/ui CSS variable convention so shadcn components pick up the theme automatically.

    Update src/app/layout.tsx: Add `className="dark"` to the html element. Set body className to use bg-background and text-foreground. Add metadata: title "AurumIQ", description "AI-Powered Financial Analysis Platform".

    Update src/app/page.tsx: Simple centered text "AurumIQ" with a redirect-to-login hint (temporary landing). No complex layout needed.

    Run `npx prisma generate` then `npx prisma db push` to create the users table in the local PostgreSQL database.
  </action>
  <verify>
    <automated>cd "C:/Users/97254/AurumIQ 2.0" && npx prisma db push --accept-data-loss 2>&1 | tail -3 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains "model User" with fields: id, email, hashedPassword, name, createdAt, updatedAt
    - src/lib/env.ts imports createEnv from "@t3-oss/env-nextjs" and validates DATABASE_URL, NEXTAUTH_SECRET
    - src/lib/db.ts exports a const named `db` of type PrismaClient
    - src/app/globals.css contains "--background" and "--positive" and "--negative" CSS custom properties
    - src/app/layout.tsx html element has className containing "dark"
    - .env.example contains DATABASE_URL, NEXTAUTH_SECRET, NEXTAUTH_URL
    - `npx prisma db push` exits 0 (users table created)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Prisma User model pushed to PostgreSQL, typed env validation active, dark financial theme CSS tokens applied, root layout renders dark background. The skeleton's data layer and visual foundation are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> runtime | Environment variables cross from OS into Node.js process |
| browser -> server | Future auth requests will cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local | mitigate | .gitignore includes .env.local; .env.example has no secrets; @t3-oss/env-nextjs validates server-only vars never leak to client bundle |
| T-01-02 | Tampering | npm installs | mitigate | pnpm strict mode; lockfile committed; all packages are well-known (next, prisma, bcryptjs, zod) with npm weekly downloads >100k |
| T-01-SC | Tampering | npm installs | mitigate | All packages verified as legitimate: next (Vercel), prisma (Prisma), bcryptjs (dcodeIO), @t3-oss/env-nextjs (t3-oss), next-auth (Auth.js), zod (colinhacks) |
</threat_model>

<verification>
1. `pnpm dev` starts on localhost:3000 without errors
2. Browser shows dark background (not white) at localhost:3000
3. `npx prisma studio` shows the users table exists with correct columns
4. Remove DATABASE_URL from .env.local -> `pnpm dev` fails at startup with env validation error
</verification>

<success_criteria>
- Project scaffolded with all Phase 1 dependencies
- TypeScript strict mode enabled
- Prisma User model exists and is pushed to local PostgreSQL
- Dark theme renders (dark background, light text)
- Environment validation catches missing DATABASE_URL at startup
- Build passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md` when done
</output>
