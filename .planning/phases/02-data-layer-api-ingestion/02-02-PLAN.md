---
phase: 02-data-layer-api-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/finance/schemas.ts
  - src/lib/finance/adapters/earnings.adapter.ts
  - src/lib/finance/adapters/analyst.adapter.ts
  - src/lib/finance/adapters/options.adapter.ts
  - src/lib/finance/adapters/profile.adapter.ts
  - src/lib/finance/adapters/news.adapter.ts
  - src/app/api/finance/[ticker]/route.ts
  - src/lib/finance/__tests__/fixtures/yahoo-responses.ts
  - src/lib/finance/__tests__/adapters.test.ts
  - src/lib/finance/__tests__/cache.test.ts
  - src/lib/finance/__tests__/edge-cases.test.ts
autonomous: true
requirements:
  - INFRA-04
user_setup: []

must_haves:
  truths:
    - "Fetching any data type (price, earnings, analyst, options, profile, news) for a ticker stores data in raw_data with correct data_type"
    - "Each adapter fetches only its specific Yahoo Finance modules -- no over-fetching"
    - "ETF tickers return available data gracefully (no crash on missing earnings)"
    - "ADR tickers return available data gracefully"
    - "Cache hit for any data type returns stored result without external call"
    - "Unit tests cover partial responses, missing options chain, ETF vs stock vs ADR, and fallback paths"
  artifacts:
    - path: "src/lib/finance/adapters/earnings.adapter.ts"
      provides: "Earnings history + estimates fetch with cache"
      exports: ["fetchEarnings"]
    - path: "src/lib/finance/adapters/analyst.adapter.ts"
      provides: "Analyst ratings + recommendations fetch with cache"
      exports: ["fetchAnalyst"]
    - path: "src/lib/finance/adapters/options.adapter.ts"
      provides: "Options chain data fetch with cache"
      exports: ["fetchOptions"]
    - path: "src/lib/finance/adapters/profile.adapter.ts"
      provides: "Company profile + key stats fetch with cache"
      exports: ["fetchProfile"]
    - path: "src/lib/finance/adapters/news.adapter.ts"
      provides: "News headlines via yahoo-finance2 search"
      exports: ["fetchNews"]
  key_links:
    - from: "src/app/api/finance/[ticker]/route.ts"
      to: "src/lib/finance/adapters/*.adapter.ts"
      via: "dynamic adapter dispatch by dataType param"
      pattern: "fetch(Earnings|Analyst|Options|Profile|News)"
    - from: "src/lib/finance/adapters/earnings.adapter.ts"
      to: "src/lib/finance/cache.ts"
      via: "getCache/setCache"
      pattern: "getCache|setCache"
---

<objective>
Complete all remaining financial data adapters (earnings, analyst, options, profile, news) and wire them into the API route. Comprehensive test coverage for all adapters including edge cases (ETF, ADR, partial data, fallback paths).

Purpose: After this plan, every data type needed by Phases 3-8 dashboard sections is available through a single consistent API. INFRA-04 (independent data pipelines) is fully delivered.

Output: 5 new adapters, updated API route dispatching all 6 types, test fixtures for all security types, comprehensive test suite passing.
</objective>

<execution_context>
@C:/Users/97254/AurumIQ 2.0/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/97254/AurumIQ 2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/97254/AurumIQ 2.0/.planning/PROJECT.md
@C:/Users/97254/AurumIQ 2.0/.planning/ROADMAP.md
@C:/Users/97254/AurumIQ 2.0/.planning/STATE.md
@C:/Users/97254/AurumIQ 2.0/.planning/phases/02-data-layer-api-ingestion/02-RESEARCH.md
@C:/Users/97254/AurumIQ 2.0/.planning/phases/02-data-layer-api-ingestion/02-PATTERNS.md
@C:/Users/97254/AurumIQ 2.0/.planning/phases/02-data-layer-api-ingestion/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan builds on -->

From src/lib/finance/types.ts:
- DataType = "price" | "earnings" | "analyst" | "options" | "profile" | "news"
- AdapterResult<T> = { data: T | null; fromCache: boolean; stale: boolean }
- PriceData, EarningsData, AnalystData, OptionsData, ProfileData, NewsData interfaces

From src/lib/finance/cache.ts:
- getCache(ticker: string, dataType: string, ttlMs: number): Promise<unknown | null>
- setCache(ticker: string, dataType: string, data: unknown): Promise<void>

From src/lib/finance/yahoo-client.ts:
- yahooFinance (yahoo-finance2 default export, configured)
- withRetry<T>(fn: () => Promise<T>, context: { ticker: string; module: string }, retries?: number): Promise<T>

From src/lib/finance/config.ts:
- CACHE_TTL = { price: 900000, earnings: 86400000, analyst: 43200000, options: 3600000, profile: 604800000, news: 1800000 }

From src/lib/finance/schemas.ts:
- PriceDataSchema (Zod schema for price data)
- tickerSchema (Zod regex /^[A-Z0-9.]{1,10}$/)

From src/lib/finance/adapters/price.adapter.ts (pattern to replicate):
- fetchPrice(ticker: string): Promise<AdapterResult<PriceData>>
- Pattern: cache check -> withRetry(yahoo call) -> Zod safeParse -> setCache -> return
- On error: stale cache fallback -> null

From src/lib/logger.ts:
- logger, financeLogger (Pino instances)

From src/app/api/finance/[ticker]/route.ts:
- GET handler with ticker validation, session check, adapter dispatch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 5 remaining adapters + extend Zod schemas</name>
  <files>
    src/lib/finance/schemas.ts,
    src/lib/finance/adapters/earnings.adapter.ts,
    src/lib/finance/adapters/analyst.adapter.ts,
    src/lib/finance/adapters/options.adapter.ts,
    src/lib/finance/adapters/profile.adapter.ts,
    src/lib/finance/adapters/news.adapter.ts
  </files>
  <read_first>
    src/lib/finance/adapters/price.adapter.ts (the pattern to replicate exactly),
    src/lib/finance/schemas.ts (extend with new schemas),
    src/lib/finance/types.ts (type interfaces to match),
    src/lib/finance/cache.ts (getCache/setCache signatures),
    src/lib/finance/yahoo-client.ts (withRetry signature),
    src/lib/finance/config.ts (CACHE_TTL values),
    .planning/phases/02-data-layer-api-ingestion/02-RESEARCH.md (yahoo-finance2 Module-to-Dashboard Mapping, Pattern 1)
  </read_first>
  <action>
    1. Extend src/lib/finance/schemas.ts with 5 new Zod schemas. Each schema uses .nullable().optional() on volatile fields:
       - EarningsDataSchema: earningsChart.quarterly array (date, actual, estimate), currentQuarterEstimate, financialData (currentPrice, targetMeanPrice, revenueGrowth)
       - AnalystDataSchema: recommendationTrend array (period, strongBuy, buy, hold, sell, strongSell), upgradeDowngradeHistory array (firm, toGrade, fromGrade, date)
       - OptionsDataSchema: options array containing calls/puts arrays (strike, lastPrice, impliedVolatility, openInterest, volume -- all nullable), expirationDates array
       - ProfileDataSchema: assetProfile (sector, industry, fullTimeEmployees, longBusinessSummary), defaultKeyStatistics (enterpriseValue, forwardPE, profitMargins)
       - NewsDataSchema: news array (title, publisher, link, providerPublishTime, thumbnail -- all with nullable subfields)
       Export each schema and its z.infer type.

    2. Create each adapter following the EXACT pattern from price.adapter.ts:
       - earnings.adapter.ts: Export fetchEarnings. Calls quoteSummary with modules ["earnings", "earningsTrend", "financialData"]. Uses CACHE_TTL.earnings (24h). Data type key "earnings".
       - analyst.adapter.ts: Export fetchAnalyst. Calls quoteSummary with modules ["recommendationTrend", "upgradeDowngradeHistory"]. Uses CACHE_TTL.analyst (12h). Data type key "analyst".
       - options.adapter.ts: Export fetchOptions. Calls yahooFinance.options(ticker) (NOT quoteSummary -- options uses a different endpoint). Uses CACHE_TTL.options (1h). Data type key "options". Extra handling: many tickers have no options chain (ETFs, ADRs) -- catch and return null gracefully.
       - profile.adapter.ts: Export fetchProfile. Calls quoteSummary with modules ["assetProfile", "defaultKeyStatistics"]. Uses CACHE_TTL.profile (7d). Data type key "profile".
       - news.adapter.ts: Export fetchNews. Calls yahooFinance.search(ticker, { newsCount: 20 }) (NOT quoteSummary -- news uses search endpoint). Uses CACHE_TTL.news (30min). Data type key "news".
    
    Each adapter: cache check first, withRetry wrapping the yahoo call, Zod safeParse (warn on failure but store raw), stale cache fallback on error, return AdapterResult.
  </action>
  <acceptance_criteria>
    - src/lib/finance/schemas.ts exports EarningsDataSchema, AnalystDataSchema, OptionsDataSchema, ProfileDataSchema, NewsDataSchema
    - Each adapter exports a single fetch function matching the signature fetchX(ticker: string): Promise of AdapterResult
    - earnings.adapter.ts calls quoteSummary with modules ["earnings", "earningsTrend", "financialData"]
    - analyst.adapter.ts calls quoteSummary with modules ["recommendationTrend", "upgradeDowngradeHistory"]
    - options.adapter.ts calls yahooFinance.options(ticker) not quoteSummary
    - profile.adapter.ts calls quoteSummary with modules ["assetProfile", "defaultKeyStatistics"]
    - news.adapter.ts calls yahooFinance.search(ticker, { newsCount: 20 }) not quoteSummary
    - All adapters use withRetry, getCache/setCache, and Zod safeParse
    - `pnpm build` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 adapters exist with consistent patterns. Each fetches only its specific Yahoo Finance modules. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire API route for all types + comprehensive test suite</name>
  <files>
    src/app/api/finance/[ticker]/route.ts,
    src/lib/finance/__tests__/fixtures/yahoo-responses.ts,
    src/lib/finance/__tests__/adapters.test.ts,
    src/lib/finance/__tests__/cache.test.ts,
    src/lib/finance/__tests__/edge-cases.test.ts
  </files>
  <read_first>
    src/app/api/finance/[ticker]/route.ts (current state from Plan 01),
    src/lib/finance/adapters/price.adapter.ts,
    src/lib/finance/adapters/earnings.adapter.ts (just created),
    src/lib/finance/adapters/analyst.adapter.ts (just created),
    src/lib/finance/adapters/options.adapter.ts (just created),
    src/lib/finance/adapters/profile.adapter.ts (just created),
    src/lib/finance/adapters/news.adapter.ts (just created),
    src/lib/finance/__tests__/fixtures/yahoo-responses.ts (extend),
    src/lib/finance/__tests__/price-pipeline.test.ts (pattern to follow),
    vitest.config.ts
  </read_first>
  <action>
    1. Update src/app/api/finance/[ticker]/route.ts: Replace the single-type dispatch with a map from DataType to adapter function. Import all 6 fetch functions. Create const ADAPTERS: Record of DataType to fetch function = { price: fetchPrice, earnings: fetchEarnings, analyst: fetchAnalyst, options: fetchOptions, profile: fetchProfile, news: fetchNews }. On GET, look up ADAPTERS[dataType], call it, return result. Remove the "Unsupported data type" fallback for known types. Keep 400 for truly unknown types.

    2. Extend src/lib/finance/__tests__/fixtures/yahoo-responses.ts: Add mock response objects for all data types: validEarningsResponse (with quarterly array, 4 quarters of data), validAnalystResponse (with recommendationTrend for 4 periods), validOptionsResponse (with calls and puts arrays), emptyOptionsResponse (empty arrays, simulating ETF), validProfileResponse (with sector, industry, employees), validNewsResponse (with 5 news items). Also add: etfQuoteSummaryResponse (SPY-like, no earnings/analyst modules), adrQuoteSummaryResponse (TSM-like, partial fields).

    3. Create src/lib/finance/__tests__/adapters.test.ts: Test each adapter independently (mock yahoo-finance2 and db):
       - "fetchEarnings returns quarterly earnings data on cache miss"
       - "fetchAnalyst returns recommendation trend on cache miss"
       - "fetchOptions returns calls and puts on cache miss"
       - "fetchOptions returns null for ticker without options chain" (mock to throw, no stale cache)
       - "fetchProfile returns sector and key stats on cache miss"
       - "fetchNews returns articles on cache miss"
       Each test: mock quoteSummary/options/search to return fixture, verify setCache called with correct dataType, verify result.data contains expected fields.

    4. Create src/lib/finance/__tests__/cache.test.ts: Test cache module directly (mock db):
       - "getCache returns null when no matching record" (findFirst returns null)
       - "getCache returns data within TTL window" (findFirst returns row with recent fetchedAt)
       - "getCache returns null for expired cache" (findFirst returns null because fetchedAt < cutoff)
       - "setCache upserts with uppercased ticker" (verify upsert called with ticker.toUpperCase())
       - "setCache updates fetchedAt on existing record" (verify upsert update path)

    5. Create src/lib/finance/__tests__/edge-cases.test.ts: Test cross-cutting edge cases:
       - "ETF ticker (SPY) returns price data but null for earnings" -- mock price to succeed, earnings to return partial/null
       - "ADR ticker (TSM) returns price with nullable fields" -- mock partial response
       - "Ticker with special chars (BRK.B) passes validation" -- tickerSchema.safeParse("BRK.B").success === true
       - "All adapters fall back to stale cache when Yahoo Finance is down" -- mock all quoteSummary calls to throw, verify stale fallback for each adapter
       - "Concurrent fetches for same ticker share cache" -- call fetchPrice twice rapidly, verify quoteSummary called only once (second hits cache)
  </action>
  <acceptance_criteria>
    - API route dispatches all 6 data types to their respective adapters
    - API route returns 400 for unknown dataType values
    - fixtures/yahoo-responses.ts has mock responses for all 6 data types plus ETF and ADR variants
    - adapters.test.ts has 6 passing tests (one per adapter)
    - cache.test.ts has 5 passing tests covering TTL, upsert, and miss scenarios
    - edge-cases.test.ts has 5 passing tests covering ETF, ADR, special chars, stale fallback, concurrent access
    - `pnpm vitest run src/lib/finance/__tests__/ --reporter=verbose` exits 0 with all tests green
    - Total test count across all Phase 2 test files >= 22 (6 from Plan 01 + 16 from this task)
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/lib/finance/__tests__/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 6 adapters wired to API route. 22+ tests pass covering cache behavior, all adapters, ETF/ADR/partial responses, fallback paths. INFRA-04 fully delivered -- each dashboard section can independently fetch its data type through the adapter layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API route | Untrusted dataType query param (new surface vs Plan 01) |
| Yahoo Finance -> each adapter | Each adapter receives potentially different response shapes per security type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | dataType query param | mitigate | Validate dataType against DataType union before dispatch; unknown types return 400 |
| T-02-08 | Information Disclosure | Options adapter for tickers without options | mitigate | Return null gracefully, not error details about what data exists/doesn't exist |
| T-02-09 | Denial of Service | Fetching all 6 types simultaneously | accept | Low risk for demo; each adapter is independent and cached; Phase 7 adds rate limiting |
| T-02-SC | Tampering | npm installs | mitigate | No new packages in Plan 02; all dependencies from Plan 01 already verified |
</threat_model>

<verification>
1. `pnpm build` passes
2. `pnpm vitest run src/lib/finance/__tests__/ --reporter=verbose` -- all 22+ tests green
3. API route handles all 6 data types: price, earnings, analyst, options, profile, news
4. Phase 2 success criteria from ROADMAP.md:
   - SC-1: Fetching ticker stores price, earnings, analyst, news in raw_data (verified by adapters.test.ts)
   - SC-2: NaN/null fallback with structured warning (verified by price-pipeline.test.ts partial response test + edge-cases.test.ts)
   - SC-3: Cache hit returns stored result without external call (verified by cache.test.ts + price-pipeline.test.ts cache hit test)
   - SC-4: Partial responses, ETF/stock/ADR, fallback paths tested (verified by edge-cases.test.ts)
</verification>

<success_criteria>
- All 6 financial data adapters functional with independent cache TTLs
- API route dispatches to correct adapter based on dataType param
- 22+ automated tests pass covering all adapters, cache, and edge cases
- ETF and ADR tickers handled gracefully (no crashes, partial data returned)
- INFRA-04 fully delivered: each dashboard section has its own independent data pipeline
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-api-ingestion/02-02-SUMMARY.md` when done
</output>
